{
  "name": "unsaid",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "node --test tests/",
    "web": "pnpm --filter @unsaid/web dev",
    "mobile": "pnpm --filter @unsaid/mobile start"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": { "node": ">=20" }
}
